apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: test-web-secure-route
  namespace: test
spec:
  parentRefs:
    - name: scdb-gateway
      namespace: default
      sectionName: https-test-web
  hostnames:
    - test-web.prod.home.dblanchet.net
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: test-web-svc
          port: 80
