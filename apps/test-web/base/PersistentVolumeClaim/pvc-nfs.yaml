apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: html-nfs
  namespace: test
spec:
  storageClassName: nfs-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
