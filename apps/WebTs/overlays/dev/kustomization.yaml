apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - PersistentVolumeClaim/db-backup.yml
  - HTTProutes/webts-secure-route.yml
  - ReferenceGrants/allow-gateway-to-certs.yaml

#patchesStrategicMerge:
#  - Deployements/nginx-php-patch.yaml
